# /apps — deployable surfaces

Applications that compose modules into a running product (web, admin). Apps sit at the top of
the dependency graph: they depend on modules, and nothing depends on them.

**Nothing here is implemented.**
