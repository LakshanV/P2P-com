# /design-system — shared UI primitives

The shared JAYA design system described in `JAYA_MASTER_AUTONOMOUS_DEV_GUIDE_v3` §34:
typography, spacing, layout, controls, chat, commerce cards, status chips, tables, charts,
media, motion and accessibility primitives.

**Not implemented.** Build step B-4 (FND-006).

Depth 2 in the layer ordering: importable by any module or app, and never by the kernel.
